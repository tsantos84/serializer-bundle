filter:
    excluded_paths:
      - 'Tests/*'

build:
    environment:
        php:
            version: 7.2

    tests:
        override:
            -
                command: vendor/bin/phpunit --coverage-clover=coverage-file -v
                coverage:
                    file: coverage-file
                    format: php-clover
